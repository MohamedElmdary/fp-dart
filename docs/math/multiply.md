# multiply

```dart
double multiply(num x, num y)
```

Multiplies two numbers.

```dart
multiply(0, 1); // 0
multiply(1, 1); // 1
multiply(-1, 1); // -1
multiply(-1, double.infinity); // double.negativeInfinity
```
