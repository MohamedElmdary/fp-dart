# subtract

```dart
double subtract(num x, num y)
```

Subtracts its second argument from its first argument

```dart
subtract(1, 1); // 0
subtract(0, 1); // -1
subtract(1, 0); // 1
subtract(-1, 1); // -2
subtract(1, -1); // 2
```
